{
  "name": "claude-mini-hud",
  "version": "1.0.5",
  "description": "Claude Code StatusLine plugin — context / token / todos / tools / agents + 21 progress bar themes + multi-provider usage (Claude/MiniMax/Zhipu/MiMo/DeepSeek/Kimi/...)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "claude-mini-hud": "dist/index.js"
  },
  "files": [
    "dist/",
    "commands/",
    ".claude-plugin/"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run build && npm run typecheck && node --import tsx --test tests/stdin.test.ts tests/usage.test.ts",
    "test:stdin": "npm run build && echo '{\"model\":{\"display_name\":\"MiniMax-M3\"},\"context_window\":{\"current_usage\":{\"input_tokens\":45000},\"context_window_size\":200000},\"transcript_path\":\"/tmp/test.jsonl\"}' | node dist/index.js"
  },
  "keywords": [
    "claude-code",
    "statusline",
    "hud",
    "minimal"
  ],
  "author": "Zander Zhang",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}